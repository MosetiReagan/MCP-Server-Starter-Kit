# My MCP Server

Run in development:

```bash
npm run dev
```

The HTTP MCP endpoint is available at `http://127.0.0.1:3000/mcp`.

Add tools in `src/server.ts`, configuration in `src/config.ts`, and copy examples into `src/tools`, `src/resources`, or `src/prompts` as your project grows.
